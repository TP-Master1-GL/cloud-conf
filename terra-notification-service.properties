# Server Configuration
server.port=4002

# Application Name
spring.application.name=terra-notification-service

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/notification
spring.datasource.username=john-nguembu
spring.datasource.password=

# RabbitMQ Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Notification Specific RabbitMQ
notification.rabbitmq.exchange=user.events
notification.rabbitmq.queue=queue.user.created
notification.rabbitmq.routing-key=user.created

# Email Configuration (AVEC VOTRE VRAI APP PASSWORD)
spring.mail.host=smtp.gmail.com 
spring.mail.port=587
spring.mail.username=terrabia237@gmail.com
spring.mail.password=ghxy dqxg axgz votf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Eureka Configuration
eureka.client.service-url.defaultZone=http://terra-registry-service:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# Logging
logging.level.com.terrabia.notification=DEBUG
logging.file.name=logs/terra-notification-service.log
